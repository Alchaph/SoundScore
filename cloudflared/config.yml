# Cloudflare Tunnel configuration
# For use with the --profile tunnel Docker Compose override
# Replace <your-tunnel-id> with your Cloudflare Tunnel UUID

tunnel: <your-tunnel-id>
no-autoupdate: true

ingress:
  # All traffic → Caddy on the Docker network
  - service: http://caddy:8888
